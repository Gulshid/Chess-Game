gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# Firebase config — these are tied to a specific Firebase project (yours),
# so keep them out of version control rather than committing them for
# every contributor/CI runner to regenerate via `flutterfire configure`.
google-services.json
**/google-services.json